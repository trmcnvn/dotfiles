set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'

# Prefix
unbind C-b
set -g prefix C-\;
bind \; send-prefix

# Reload config
bind q source-file ~/.config/tmux/tmux.conf

# Vi mode for copy
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Pane Controls
bind h split-window -h -c "#{pane_current_path}"
bind v split-window -v -c "#{pane_current_path}"

# Smart pane switching with Vim awareness.
vim_pattern='(\S+/)?g?\.?(view|l?n?vim?x?|fzf)(diff)?(-wrapped)?'
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +${vim_pattern}$'"

bind -n C-M-Left if-shell "$is_vim" "send-keys C-M-Left" "select-pane -L"
bind -n C-M-Right if-shell "$is_vim" "send-keys C-M-Right" "select-pane -R"
bind -n C-M-Up if-shell "$is_vim" "send-keys C-M-Up" "select-pane -U"
bind -n C-M-Down if-shell "$is_vim" "send-keys C-M-Down" "select-pane -D"

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

# Window navigation
bind r command-prompt -I "#W" "rename-window -- '%%'"
bind c new-window -c "#{pane_current_path}"
bind x kill-window

bind -n C-S-PageUp next-window
bind -n C-S-PageDown previous-window

# Session controls
bind R command-prompt -I "#S" "rename-session -- '%%'"
bind C new-session
bind X kill-session

bind -n C-M-S-PageUp switch-client -p
bind -n C-M-S-PageDown switch-client -n

# Shell
if-shell "test -f /opt/homebrew/bin/nu" "set-option -g default-shell /opt/homebrew/bin/nu" "set-option -g default-shell /usr/bin/nu"

# General
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
setw -g aggressive-resize on
set -g detach-on-destroy off

# Status bar
set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 80
set -g window-status-separator ""

# Plugins
set -g @plugin "tmux-plugins/tpm"
set -g @plugin "christoomey/vim-tmux-navigator"

set -g @plugin "tmux-plugins/tmux-resurrect"
set -g @ressurect-strategy-vim "session"
set -g @resurrect-strategy-nvim "session"
set -g @ressurect-capture-pane-contents "on"

set -g @plugin "tmux-plugins/tmux-continuum"
set -g @continuum-restore "on"
set -g @continuum-boot "on"
set -g @continuum-save-interval "10"

# Theme (Personal)
set -g status-style "fg=#87919d,bg=#141824"
set -g message-style "fg=#cbcebc,bg=#1c2748"
set -g message-command-style "fg=#141824,bg=#ffcc66"

set -g pane-border-style "fg=#21333f"
set -g pane-active-border-style "fg=#26b2b2"
set -g display-panes-colour "#ffcc66"
set -g display-panes-active-colour "#cbcebc"

set -g window-status-style "fg=#87919d,bg=#141824"
set -g window-status-current-style "fg=#ffcc66,bg=#141824,bold"
set -g window-status-activity-style "fg=#f8ad34,bg=#141824"
set -g window-status-bell-style "fg=#ff0000,bg=#141824,bold"

set -g window-status-format "#[fg=#87919d]#I #[fg=#cbcebc]#W"
set -g window-status-current-format "#[fg=#ffcc66,bold]#I #[fg=#ffcc66,bold]#W"
set -g window-status-separator "  "

set -g mode-style "fg=#cbcebc,bg=#1c2748"

set -g status-left " "
set -g status-right "#[fg=#87919d] #[fg=#cbcebc]#{b:pane_current_path} #[fg=#{?client_prefix,#f07178,#87919d}] #[fg=#cbcebc]#S "

# Custom plugins path
set-environment -g TMUX_PLUGIN_MANAGER_PATH "~/.config/tmux/plugins"

# Install TPM
if "test ! -d ~/.config/tmux/plugins/tpm" \
  "run 'jj git clone --colocate git@github.com:tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"
run "~/.config/tmux/plugins/tpm/tpm"
