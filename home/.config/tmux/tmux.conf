set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'

# Prefix
unbind C-b
set -g prefix C-\;
bind \; send-prefix

# Reload config
bind q source-file ~/.config/tmux/tmux.conf

# Vi mode for copy
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Pane Controls
bind h split-window -h -c "#{pane_current_path}"
bind v split-window -v -c "#{pane_current_path}"

bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Down resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Right resize-pane -R 5

# Window navigation
bind r command-prompt -I "#W" "rename-window -- '%%'"
bind c new-window -c "#{pane_current_path}"
bind x kill-window

bind -n C-S-PageUp next-window
bind -n C-S-PageDown previous-window

# Session controls
bind R command-prompt -I "#S" "rename-session -- '%%'"
bind C new-session
bind X kill-session

bind -n C-M-S-PageUp switch-client -p
bind -n C-M-S-PageDown switch-client -n

# Shell
if-shell "test -f /opt/homebrew/bin/nu" "set-option -g default-shell /opt/homebrew/bin/nu" "set-option -g default-shell /usr/bin/nu"

# General
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
setw -g aggressive-resize on
set -g detach-on-destroy off

# Status bar
set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 50
set -g window-status-separator ""

# Theme (Rosé Pine)
set -g status-style "bg=#191724,fg=#e0def4"
set -g status-left "#[fg=#191724,bg=#c4a7e7,bold] #S #[bg=#191724] "
set -g status-right "#[fg=#c4a7e7]#{?client_prefix,PREFIX ,}#[fg=#6e6a86]#h "
set -g window-status-format "#[fg=#6e6a86] #I:#W "
set -g window-status-current-format "#[fg=#ebbcba,bold] #I:#W "
set -g pane-border-style "fg=#403d52"
set -g pane-active-border-style "fg=#c4a7e7"
set -g message-style "bg=#1f1d2e,fg=#ebbcba"
set -g message-command-style "bg=#1f1d2e,fg=#ebbcba"
set -g mode-style "bg=#c4a7e7,fg=#191724"
setw -g clock-mode-colour "#ebbcba"

