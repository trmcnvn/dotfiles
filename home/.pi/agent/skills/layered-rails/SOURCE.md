# Source

This skill is adapted for Pi from the `layered-rails` material in:

- https://github.com/palkan/skills
- upstream skill path: `layered-rails/skills/layered-rails/SKILL.md`
- upstream command docs: `layered-rails/commands/*.md`

The upstream repository declares MIT licensing in its README and plugin metadata.

This Pi version keeps the core layered-Rails workflow, but maps Claude-style slash commands to Pi subcommands under:

```bash
/skill:layered-rails <command>
```
