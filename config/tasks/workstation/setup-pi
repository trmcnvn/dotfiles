#!/bin/sh
#MISE description="Ensure Pi runs under the active Node runtime"
set -eu
if ! pi --version >/dev/null 2>&1; then
    npm install -g --ignore-scripts @earendil-works/pi-coding-agent
    pi --version >/dev/null
fi
